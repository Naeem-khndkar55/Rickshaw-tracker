const cloudinary = require("../config/cloudinary");

// Placeholder for image upload middleware (design only)
exports.uploadImage = (req, res, next) => {
  console.log("Upload image middleware placeholder");
  next();
};